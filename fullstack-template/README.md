# Fullstack Template

Express + MongoDB backend and React (Vite) frontend.

## Setup
### Backend
cd Backend
cp .env.example .env   # fill in your values
npm install
npm run dev

### Frontend
cd Frontend
npm install
npm run dev
